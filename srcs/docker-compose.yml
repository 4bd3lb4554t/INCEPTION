

services:

    nginx:
        depends_on:
        - wordpress
        image: image_nginx
        container_name: container_nginx
        build: ./requirements/nginx
        ports:
        -   "1337:9000"
        volumes:
            - Volumedata:/var/www/wordpress
    wordpress:
        image: image_wordpresss
        container_name: container_wordpresss
        build: ./requirements/wordpress
        ports:
        -   "9000:9000"
        volumes:
            - Volumedata:/var/www/wordpress

volumes:
    Volumedata:
